// api/index.js
// Vercel's entry point: any request Vercel routes here (see vercel.json)
// is handed to the same Express app that runs locally via `npm start`.
module.exports = require('../server');
